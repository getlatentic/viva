# Family 6 (held out): a version id out of tool prose

The friction: extracting the id a tool's prose reported, reliably, when the
prose also carries dates, token counts, casual `v9` mentions, notes that land
mid-sentence, and refusals that mint nothing. The receipt: the first test of
the capability tools parsed the word `version` instead of the number and
asked the owner to activate NIL.

The checks parse independently by the stated grammar; reference answers are
hand-written from the authored logs; the gate reconciles the two.

Held out (amendment 11): authored after the scored four were frozen, excluded
from pilots, included in the battery and the primary sign test.
